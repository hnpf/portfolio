a heads up on vercel.


we are using better-sqlite3 on a serverless platform (Vercel), our 
database lives in /tmp.

locally? persistence is permanent

with vercel? persistence is ephemeral, when the lambda "restarts" 
(which happens frequently), links will be wiped! it's sadly how SQLite 

works on serverless without persistent volume
